{
  "name": "w-log-api",
  "version": "1.0.0",
  "description": "Personal workout app",
  "main": "server.ts",
  "scripts": {
    "dev": "nodemon",
    "build": "rm -rf ./build && tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
